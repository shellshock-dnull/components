---
on:
  push:
    branches: [master]
    paths:
    - 'defaults/main/nvim.yml'
    - 'tasks/**'
    - 'molecule/nvim/**'
  pull_request:
    branches: [master]
    paths:
    - 'defaults/main/nvim.yml'
    - 'tasks/**'
    - 'molecule/nvim/**'
jobs:
  tests:
    runs-on: ubuntu-latest
    name: component:nvim
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          path: ${{ github.repository }}
      - name: Molecule
        uses: gofrolist/molecule-action@v2
        with:
          molecule_command: test
          molecule_args: -d docker -s nvim
          molecule_working_dir: dnullproject/components
        env:
          ANSIBLE_FORCE_COLOR: '1'
