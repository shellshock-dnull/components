---
on:
  push:
    branches: [master]
    paths:
    - 'defaults/clipmenu.yml'
    - 'tasks/**'
    - 'molecule/clipmenu/**'
  pull_request:
    branches: [master]
    paths:
    - 'defaults/clipmenu.yml'
    - 'tasks/**'
    - 'molecule/clipmenu/**'
jobs:
  tests:
    runs-on: ubuntu-latest
    name: component:clipmenu
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          path: ${{ github.repository }}
      - name: Molecule
        uses: gofrolist/molecule-action@v2
        with:
          molecule_command: test
          molecule_args: -d docker -s clipmenu
          molecule_working_dir: dnull-project/components
        env:
          ANSIBLE_FORCE_COLOR: '1'
