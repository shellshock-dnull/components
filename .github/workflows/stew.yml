---
name: Molecule:stew
on:
  push:
    paths:
    - 'defaults/main/stew.yml'
    - 'tasks/**'
    - 'molecule/stew/**'
  pull_request:
    branches: [master]
    paths:
    - 'defaults/main/stew.yml'
    - 'tasks/**'
    - 'molecule/stew/**'
jobs:
  tests:
    runs-on: ubuntu-latest
    name: component:stew
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          path: ${{ github.repository }}
      - name: Molecule
        uses: gofrolist/molecule-action@v2
        with:
          molecule_command: test
          molecule_args: -d docker -s stew
          molecule_working_dir: dnullproject/components
        env:
          ANSIBLE_FORCE_COLOR: '1'
