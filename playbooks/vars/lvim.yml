role_component:
  lvim:
    # https://www.lunarvim.org/docs/installation
    name: "LunarVIM"
    # deps:
      # git, make, pip, python npm, node and cargo
      # - asdf
      # - nvim
    # install:
    #   pre:
    #     - name: "[lvim] Install via official install script"
    #       shell: bash <(curl -s https://raw.githubusercontent.com/LunarVim/LunarVim/release-1.3/neovim-0.9/utils/installer/install.sh)
    #       environment:
    #         LV_BRANCH: "release-1.3/neovim-0.9"
    configs:
      templates:
        config_lua:
          template: lvim/config.lua
          dest: ~/.config/lvim/config.lua
          # variables:
