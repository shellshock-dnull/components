---
# tmux
role_component:
  tmux:
    install:
      brew:
        - tmux
    configs:
      templates:
        # main templates
        main:
          template: tmux.conf
          dest: ~/.tmux.conf
